meta {
  name: GetProfile
  type: http
  seq: 2
}

get {
  url: http://localhost:3000/profile
  body: none
  auth: bearer
}

headers {
  Authorization: Bearer
}

auth:bearer {
  token: {{accessToken}}
}

settings {
  encodeUrl: false
  timeout: 0
}
